# SimpleMCPServer 搜索指南

> 适用范围：所有需要联网检索、事实核验、资料搜集和来源对比的项目。
>
> 核心原则：**搜索结果是线索，不是证据；搜索排名不是可信度排名。**

## 一、搜索通道与分工

### 1. SimpleMCPServer：统一搜索入口

SimpleMCPServer 将多个搜索来源接入统一的 `web.search` 接口。当前通道通常按以下顺序工作：

```text
Serper（Google API）→ Google（可用时）→ Bing → DDG HTML → DDG Lite
```

适合：

- 先发现资料线索；
- 搜索论文、官方网页、新闻、档案和 PDF；
- 对比多个搜索引擎的结果。

### 2. Serper / Google

适合优先搜索：

- 英文论文和综述；
- 政府、大学和国际组织网站；
- WHO、CDC、联合国、诺贝尔奖等权威资料；
- 国际事件、历史档案和英文统计。

Serper 可用时通常是第一检索通道，但结果仍需打开原文核验。

### 3. Bing

适合：

- 补充官方页面、大学页面和 PDF；
- 搜索旧网页、新闻档案和冷门资料；
- 与 Serper/Google 结果交叉比较。

中文结果质量可能不稳定，中文事实不要只依赖 Bing。

### 4. 搜狗微信搜索

适合：

- 国内人物、政策和历史传播线索；
- 中文公众号资料和查重；
- 查找国内读者熟悉的表述。

入口：

```text
https://weixin.sogou.com/weixin?type=2&query=关键词
```

公众号文章、转载文章和自媒体内容只能作为线索。重要事实必须继续追溯到原始论文、官方文件或机构原文。

### 5. DuckDuckGo（DDG）

适合：

- 补充遗漏结果；
- 搜索冷门人物、旧档案和网页镜像；
- 检查某个结论是否只在少数搜索引擎出现。

DDG 没有搜到，不代表事实不存在；它主要用于补充和反向检查。

## 二、SimpleMCPServer 的调用方法

### 1. 项目地址

GitHub：

[https://github.com/redcool/SimpleMCPServer](https://github.com/redcool/SimpleMCPServer)

### 2. 服务地址

在本机运行时，默认通过以下地址调用：

```text
http://127.0.0.1:45678/rpc
```

调用方法是 JSON-RPC 2.0，工具名为 `web.search`。

### 3. PowerShell 示例

```powershell
$query = "Kuru disease Gajdusek official review"

$payload = @{
    jsonrpc = "2.0"
    id = 1
    method = "tools/call"
    params = @{
        name = "web.search"
        arguments = @{
            query = $query
            maxResults = 8
        }
    }
}

$json = $payload | ConvertTo-Json -Depth 6

# 关键：把 JSON 转成 UTF-8 字节后再发送
$body = [System.Text.Encoding]::UTF8.GetBytes($json)

$result = Invoke-RestMethod `
    -Uri "http://127.0.0.1:45678/rpc" `
    -Method Post `
    -ContentType "application/json; charset=utf-8" `
    -Body $body

$result.result.content[0].text
```

### 4. 中文乱码的原因

PowerShell 字符串本身通常是 Unicode，但发送 HTTP 请求时，如果直接把字符串作为请求体，可能经过系统默认代码页或错误的编码转换。中文查询会因此变成：

```text
???
```

查询词一旦变成乱码，搜索引擎得到的就不是原来的问题，结果自然会失真。

因此中文查询必须使用：

```powershell
[System.Text.Encoding]::UTF8.GetBytes($json)
```

并同时设置：

```text
Content-Type: application/json; charset=utf-8
```

### 5. 查询词写法

同一事实建议拆成三类查询：

```text
事实查询：主题 + 人物/日期/数字
来源查询：主题 + official / report / original paper / WHO / CDC
争议查询：主题 + controversy / disputed / review / criticism
```

不要只写一个很长的中文问题。先用短查询定位线索，再用来源词追权威页面。

## 三、多引擎搜索顺序与信息核对参考

### 1. 推荐搜索顺序

#### 国际事件、论文、医学和科学史

```text
Serper/Google → Bing → DDG → 原始论文/官方页面
```

#### 中国人物、政策、统计和中文传播

```text
Serper/Google → Bing → 搜狗微信 → 中国官方站点/论文
```

#### 冷门史料、旧网页和档案

```text
Serper/Google → Bing → DDG → 档案馆/大学/博物馆原文
```

### 2. 不要按“结果数量”投票

十篇转载文章不等于十个独立来源。分析结果时先做来源去重：

- 同一新闻的转载，只算一个来源链；
- 同一论文在多个数据库的索引，只算一个原始来源；
- 多篇公众号文章引用同一个百科，也只算一个来源链；
- 优先追溯最早、最直接、最权威的原文。

### 3. 参考来源层级

通常优先级如下：

1. 法律、公约、政府公报、国际组织报告、官方统计；
2. 原始论文、系统综述、大学档案、正式调查报告；
3. 专业医学机构、疾控机构、学术出版社、博物馆；
4. 高质量新闻调查、人物传记、历史综述；
5. 普通科普网站、百科、公众号、论坛。

第 4、5 类可以帮助发现线索，但重要事实不能只依赖它们。

### 4. 四项核对法

对每个重要事实，至少核对：

- **时间**：事件发生日期和文章发布时间是否混淆；
- **定义**：病例、感染者、死亡者、估计数、报告数是不是同一个概念；
- **范围**：全球、国家、地区、机构的数据有没有混用；
- **因果**：来源说的是“证明”“支持”“认为”还是“推测”。

### 5. 来源对照表

建议在项目证据卡中记录：

| 主张 | Serper/Google | Bing | 搜狗微信 | DDG | 原始/权威来源 | 最终处理 |
|---|---|---|---|---|---|---|
| 人物贡献 |  |  |  |  |  | 写实/保守/存疑 |
| 日期 |  |  |  |  |  | 采用哪一口径 |
| 数字 |  |  |  |  |  | 注明年份和定义 |
| 因果关系 |  |  |  |  |  | 证明/支持/认为/推测 |

### 6. 进入正文前的最低要求

重要事实至少满足以下一项：

- 有直接、明确的官方或原始来源；
- 有两份以上独立的高质量来源，结论一致；
- 有官方资料与学术资料交叉，且统计口径一致；
- 存在争议，但主流解释清楚，正文使用保守限定词。

以下情况不能直接写成确定事实：

- 只有搜索摘要；
- 只有一篇公众号或自媒体文章；
- 多个网页实际上都是同一篇文章的转载；
- 数字没有年份、地区或统计定义；
- “第一”“唯一”“首创”等只有宣传性表述；
- 因果关系只来自标题或推测；
- 当代敏感事件只有匿名截图或无法追溯的消息。

### 7. 最终原则

> 不要寻找“最符合预设观点”的结果，要寻找“最经得起追问”的来源。
>
> 搜索引擎负责帮我们发现路，原始资料负责告诉我们路是否真的存在。
